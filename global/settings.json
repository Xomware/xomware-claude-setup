{
  "$comment": "Hooks are NOT defined here. They ship with the xomware plugin at plugins/xomware/hooks/hooks.json. Declaring them in both places fires every hook twice. Rules are not here either — they live in global/rules/ and are symlinked into ~/.claude/rules/ by xom-install-claude-setup.",
  "permissions": {
    "deny": [
      "Bash(rm -rf *)",
      "Bash(git push --force *)",
      "Bash(git push * --force *)",
      "Bash(git reset --hard *)",
      "Bash(git clean -fd *)",
      "Bash(git clean -f *)",
      "Read(.env)",
      "Read(.env.*)",
      "Read(**/.env)",
      "Read(**/.env.*)",
      "Read(**/secrets/**)"
    ]
  },
  "enabledPlugins": {
    "xomware@xomware": true
  },
  "model": "opus[1m]",
  "subagentModel": "sonnet",
  "alwaysThinkingEnabled": true,
  "showThinkingSummaries": true,
  "outputStyle": "Concise",
  "includeCoAuthoredBy": false,
  "attribution": {
    "commit": "",
    "pr": ""
  }
}
