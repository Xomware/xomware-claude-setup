# /setup

Walk me through setting up Claude Code for this project step by step.

1. Check if `~/.claude/CLAUDE.md` exists. If not, tell me to run `bash install.sh` first.
2. Check if `.claude/CLAUDE.md` exists in current project. If not, tell me to run `init-project.sh`.
3. Read the current `.claude/CLAUDE.md` — identify any unfilled template placeholders (text in [brackets]).
4. Ask me to fill in each placeholder: project name, stack, key commands, important paths.
5. Update the file with my answers.
6. Read `docs/workflows/feature-workflow.md` and give me a 5-line summary of the workflow.
7. Confirm setup is complete and tell me to run `/catchup` to start.

$ARGUMENTS
