---
name: plan
description: >
  Turn an idea or a chosen brainstorm option into a written plan doc at
  docs/features/[topic]/PLAN.md, via the planner agent. Use before implementing any
  non-trivial feature. Do not use for tiny tasks under 30 minutes — use /fix instead.
argument-hint: [topic]
---

# /plan

Use the planner agent to break down the task before writing any code.

$ARGUMENTS
